# 노션/OneDrive 원본 대비 변경사항 (행동·시나리오·아키텍처)

> 원본(`노션_정리.md` A1~A21·B1~B20·C1~C16, `아이디에이션_OneDrive통합.md`) → **현재(실측 반영)** 변경 정리.
> 태그: 🆕 신규 · 🔬 개념→실측 · 🔧 정정 · 📊 결과추가

---

## 1. 공격 행동 (Action)

| 항목 | 변경 |
|------|------|
| 🆕 Inject(주입) | 카탈로그에 없던 *은밀 가용성 드레인* 추가(합성 오버레이) — fig45 |
| 🆕 TargetLeader·ExploitKnown·FloodAll | 코드팀 신규 액션 3종 반영(action_info.csv) |
| 🔬 Exploit 표적 효과 | 단독 실측: 랜덤 1.00(최강)·근접 0.75·원거리 0.375 — *표적 선택이 큰 차이* |
| 🔬 재밍/차단 | 단독 점령 0.25/0.375 — *점령엔 약하나 가용성 공격*임을 수치로 확정 |

## 2. 방어 행동 (Action)

| 항목 | 변경 |
|------|------|
| 🔬 RetakeSuspicious | 단독 점령 **0.0(최강)** — "표적 재장악"이 방어의 핵심임을 실측 |
| 🔧 DeployDecoy | **sim 기본 no-op(무효)** 확인 → 작동 시 효과(웜 0.84→0.05)로 정량화 |
| 🔧 Analyse | 관측으로 강등였음 → *합성 오버레이*로 은닉 주입 점검 구현(fig45) |
| 🆕 Failsafe | 자율 안전모드(중앙 단절 대비) 정식 추가 |

## 3. 공격 시나리오 (Scenario)

| 항목 | 변경 |
|------|------|
| 🆕 **A-MV 동시 멀티벡터** | *신규 1급 시나리오* — 웜+재밍+차단 동시(점령 막아도 가용성 2배 피해) |
| 🆕 **A-CONN 연결성 단절** | *신규 최적 공격* — 허브·절단점 분할(곱셈 0.85→0.31, 우리 방어 유일 약점) |
| 🔬 A1~A21 전수 측정 | 23종을 메커니즘 매핑해 코디네이터 상대 전수 실측(fig60), 모델 불가 0 |
| 📊 위협 4분류 확정 | 점령/가용성/탐지/연결성 — **위협이 연결성·가용성에 집중** |
| 🔬 A18 SwarmFuzz | 개념→실측(0.18→1.00, ×5.45) |
| 🔧 A12·A15·A16 | "모델 불가"였으나 프록시로 측정(사이드채널 0.895·내부자 0.803·ISR탈취 0.898) |

## 4. 방어 시나리오 (Scenario)

| 항목 | 변경 |
|------|------|
| 🆕 **코디네이터(역할배분 CTDE)** | *최고 방어* 신규 — 곱셈 0.737(90회 CI ±0.035) |
| 🆕 MPC(self-시뮬레이션)·분기율 라우터·가용성 인지(C)·분산 자율 | 신규 에이전틱 방어 |
| 🔬 B6 기만 | 개념→실측(작동 디코이 0.84→0.05) |
| 🔬 B1~B20 전수 매핑 | 효과는 *코디네이터 약점 보강*(가용성·Sybil·연결성)에만 — fig61 |
| 🔧 천장의 해답 | "연결성=막다른 천장"이 아니라 **2-연결성 + 분산 자율로 돌파 가능**(fig59) |

## 5. 에이전트 아키텍처 (C1~C16)

| 항목 | 변경 |
|------|------|
| 🔬 C9 계층 MARL(CTDE) | = **코디네이터(0.737, 최고)로 실현** |
| 🔬 C13 월드모델 | = **MPC(0.779)로 실현** |
| 🔬 C8 GNN·C10 DT | **학습 파이프라인 구축·실측**(GNN 0.691·DT 0.661) |
| 📊 C12 LLM-as-Judge | 시연(코디네이터 전략과 일치)·실시간 비현실 → 오프라인 증류 결론 |
| 🔧 RL(강화학습) | from-scratch REINFORCE *실패*(0.286) → "순수 학습 < 휴리스틱" 재확증 |

## 6. 메트릭 (신규)

| 항목 | 변경 |
|------|------|
| 🆕 가용성 곱셈 종합점수 | 대회식 `(공격+방어)×가용성` 정합(과잉방어 처벌) |
| 🆕 gap-closed%·best-response exploitability·지연·확장성 | 정규화·견고성·효율 보강 |
| 🔧 팀 메트릭 정합 | comp_F1=0.866(대시보드 기준)으로 A/D 재현 |

---

## 한 줄 요약 (변경의 본질)
> 원본은 *카탈로그(아이디어)*였고, 현재는 **그 전부를 실측**해 — ① 신규 위협 2종(동시·연결성), ② 신규 최고 방어
> (코디네이터), ③ 천장의 해답(2-연결성+분산자율), ④ 학습/RL 한계 확인까지 — *숫자와 결론*을 붙인 것이다.
