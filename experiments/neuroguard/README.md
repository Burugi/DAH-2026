# NeuroGuard — 공방 실측 연구 (experiments)

팀 NeuroGuard가 CybORG DroneSwarm(CC3) 위에서 돌린 **AI 공격↔방어 자율 공방 실측**의 코드·결과·문서. 대회 채점식
`(공격+방어)×가용성`에 맞춘 **가용성 곱셈종합**으로 평가.

> 이 폴더는 기존 `src/`·`docs/`를 **변경하지 않고 추가만** 한다. (별도 예외: `src/sim/fleet.py`의 IndexError 버그픽스 1건 포함 — 아래.)

## 📂 구조
| 경로 | 내용 |
|------|------|
| `docs/` | 문서 — 제출 보고서(`예선보고서_최종통합`), 시나리오·코드 연계(`시나리오_설명_코드연계_팀공유`), 추가연구(`추가연구_특이점과한계돌파`), 마스터 인덱스(`00_제출본_인덱스_및_종합요약`) 등 md+docx 52쌍 |
| `exp/` | 실험 — `exp_*.py`(79개, 공격·방어·메트릭·특이점·탐지/임무), `summary_*.csv`(75), `fig*.png`(62) |

## ▶ 재현
```powershell
& <venv>\python.exe exp\exp_matrix.py     # 전 시나리오×방어 매트릭스 → summary_matrix.csv
```
- **시나리오 정의의 단일 진실 = `exp/exp_matrix.py`의 `SCEN` 리스트.** 시나리오 추가는 `(이름, spec)` 한 줄.
- ⚠️ 스크립트는 원 개발환경의 **절대경로**(`C:\workspace\...`, 팀 레포 `src`)를 가정한다. 다른 환경에선 상단 경로 상수만 조정하면 된다.

## 🔧 포함된 버그픽스
`src/sim/fleet.py` `generate_fleet()`: 공격 `targets` 인덱스가 fleet 크기 `n`을 넘으면 `pos_rep[t][tgt]`에서 IndexError가
났다(소형 fleet에서 gps_spoof `targets:[12,13]` 등). → **`tgt`를 `< n`으로 필터링**(빈 경우 skip)해 안전하게 수정.

## 🎯 핵심 결론 (실측)
1. 위협은 **연결성·가용성**에 집중. 전 23시나리오 코디네이터 곱셈 **0.736**, 통합방어 **0.779**.
2. ML로 통합방어를 깨는 **특이점(웜+대량 허브분할)** 자동 발굴 → 반응형·정적 방어 실패 → **동적 형상 재구성(v2)**으로 해결(0.24→0.74).
3. **탐지 오라클·메트릭 맹점** 두 한계 실측 → 지속 스텔스는 정보이론적 탐지 불가(→**봉쇄**), 그레이존 오임무는 메트릭 맹점(→**임무 무결성 축**).
4. 명시 설계 > 모방 > from-scratch RL(0.286 실패). 정직한 null(cusum·committed·reserve)도 기록.

> 팀 공유 시작점: `docs/시나리오_설명_코드연계_팀공유` · 전체 색인: `docs/00_제출본_인덱스_및_종합요약`.
